<?xml version="1.0" encoding="utf-8"?>
<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="rectangle">

    <!-- Borde negro -->
    <stroke
        android:width="2dp"
        android:color="#F8C595" />

    <!-- Fondo blanco -->
    <solid
        android:color="@android:color/white" />

    <!-- Esquinas redondeadas -->
    <corners
        android:radius="30dp" />
</shape>